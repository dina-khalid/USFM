name: "usfm_ft_fold0"
type: "SegBase" # SegBase or SegVOS
num_classes: 2 # 2 for [0,1], 3 for [0,1,2], etc.
img_size: 512
path:
  root: "./datasets/Seg/Thyroid_USFM_fold0"
  split:
    train: "training_set"
    val: "val_set"
    test: "test_set"
    vis:
      samples: 16
      interval: 10

transform:
